spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
          ddl-auto: create-drop
      #spring.jpa.hibernate.ddl-auto=update
      generate-ddl: true
      show-sql: true